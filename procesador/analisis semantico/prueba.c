//Ejemplo    
h(w : real) : real = mat2(0,0)[0][0];

plot f, g;  // Las primeras variables siempre van a ser las que recorren el cuadrado [0,1]x[0,1], 
            //el resto es para controlar las homotopías (modificables en tiempo de ejecución)
